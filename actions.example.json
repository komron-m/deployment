{
  "configs": {
    "working_branch": "master",
    "access_token": "4a3c31446c91f311c99b4182392a5eeee145ee90",
    "path_to_ssh_key": "/var/deployment-keys/id_rsa",
    "repository_path": "/var/www/awesome-project",
    "github_url": "https://api.github.com/repos/%user%/%awesome-project%/commits/${working_branch}"
  },

  "actions": {
    "Cleaning up": {
      "Clean branch": "git checkout .",
      "Delete untracked files": "git clean -fd",
      "Switch to working branch": "git checkout ${working_branch}"
    },
    "Add ssh-key and pull from github": {
      "Add ssh-key to agent": "ssh-add ${path_to_ssh_key}",
      "Pull from remote origin": "git pull origin ${working_branch}"
    },
    "Install dependencies": {
      "Install dependencies": "composer install --no-interaction --ignore-platform-reqs",
      "...": "some other stuff..."
    }
  }
}
